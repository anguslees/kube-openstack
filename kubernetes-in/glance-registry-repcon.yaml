kind: ReplicationController
apiVersion: v1beta1
id: &name glance-registry
labels:
  name: *name
desiredState:
  replicas: 1
  replicaSelector:
    name: *name
  podTemplate:
    labels:
      name: *name
    desiredState:
      manifest:
        version: v1beta1
        containers:
          - name: *name
            image: localhost:4999/glance
            env:
              - key: DB_URI
                value: %glance_db_uri%
              - key: RABBITMQ_PASSWORD
                value: %rabbitmq_password%
              - key: ADMIN_PASSWORD
                value: %glance_admin_password%
            command: ["/usr/local/bin/glance-registry.sh"]
            # fixme: livenessProbe?
